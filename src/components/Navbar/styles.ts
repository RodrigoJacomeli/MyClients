import styled from 'styled-components';

export const Container = styled.div`
  height: 70px;
  width: 100vw;
  background: #202020;
  position: relative;
  box-shadow: 0 0 rgba(60,60,60,0.7);
  display: flex;
  align-items: center;
  padding-left: 20px;
`;
